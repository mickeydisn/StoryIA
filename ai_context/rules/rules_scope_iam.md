### **Scope** you must ignore:

- `**/_*/**` directories
- `**/database/reference/**/*.md` files
- `**/.*/**` directories, expect `**/.ai/**`

### **Scope** you can update:

    You are allow to update directory and files in the following scope only

- `**/database/entry/**/*.md` files
